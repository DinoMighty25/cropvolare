# Development / test dependencies (not needed at runtime)
-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
pypdf>=4.0      # PDF text extraction for report content assertions
flask>=3.0      # GCS web app tests (runtime dep on the Pi only)
